/* global describe */

describe('exampleApp Services', function() {
    'use strict';

    describe('#loginService', function() {

        // var loginService;

        // beforeEach(module('angularLoginApp'));
        // beforeEach(inject(function(_loginService_) {
        //     loginService = _loginService_;
        // }));

        // describe('#login', function() {
            // it('should return null when argument is not an array', function() {
            //     var map = angelloHelper.buildMap({name: 'sergio'}, 'name');
            //     expect(map).toBeNull();
            // });

            // it('should return empty aray when property not found in the array', function() {
            //     var map = angelloHelper.buildMap([{name: 'sergio'}], 'xxx');
            //     expect(angular.isArray(map)).toBe(true);
            //     expect(map.length).toEqual(0);
            // });

            // it('should return object associated with key ', function() {
            //     var numbers = [{name: 'zero'}, {name: 'one'}, {name: 'two'}, {name: 'three'}];
            //     var map = angelloHelper.buildMap(numbers, 'name');
            //     var keys = ['zero', 'one', 'two', 'three'];
            //     for (var i = 0; i < numbers.length; i++) {
            //         expect(map[keys[i]]).toBe(numbers[i]);
            //     }
            // });
    //     });
    });

});
